# ============================================================
# CONNECT - Production & Development Dependencies
# Python 3.12+ / Django 6.0.5
# ============================================================

# Core Django & REST Framework
Django==6.0.5
djangorestframework==3.17.1
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# ASGI, WebSockets & Channel Layer (Django Channels)
daphne==4.2.2
channels==4.3.2
channels_redis==4.3.0
redis==7.4.0
Twisted==26.4.0
autobahn==25.12.2
txaio==25.12.2
service-identity==26.1.0
cryptography==48.0.0
pyOpenSSL==26.2.0

# Database & Environment
psycopg2-binary==2.9.12
python-dotenv==1.2.2

# Static Files (Production Serving)
whitenoise==6.12.0

# Image Processing (required by accounts.User.profile_picture ImageField)
Pillow==11.3.0

# Background Tasks (Celery & Message Broker)
celery==5.6.3
kombu==5.6.2
billiard==4.2.4
amqp==5.3.1
vine==5.1.0

# Production WSGI Server (Optional Alternative)
gunicorn==25.3.0

# HTTP, APIs & Networking
requests==2.34.2
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0
urllib3==2.7.0
certifi==2026.5.20
charset-normalizer==3.4.7
idna==3.16
anyio==4.10.0
sniffio==1.3.1

# Speech Integration (AssemblyAI — used for post-meeting transcription)
assemblyai==1.0.0
pydantic==2.12.5
pydantic_core==2.41.5
typing_extensions==4.14.1
annotated-types==0.7.0

# NOTE: Gemini (translation) is called via plain HTTP REST using the
# 'requests' library (already listed above). No google-generativeai
# SDK or gRPC packages are needed — they were removed to eliminate
# the grpcio-status / protobuf dependency conflict.
